<?xml version="1.0" encoding="utf-8"?>
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="wrap_content"
    android:orientation="horizontal"
    android:gravity="center">

    <LinearLayout
        android:layout_width="@dimen/playlist_dimen"
        android:layout_height="wrap_content"
        android:orientation="vertical"
        android:layout_marginVertical="8dp">

        <androidx.cardview.widget.CardView
            android:layout_width="@dimen/playlist_dimen"
            android:layout_height="@dimen/playlist_dimen"
            app:cardCornerRadius="@dimen/corner_dimen"
            app:cardElevation="0dp">

            <ImageView
                android:id="@+id/ivPlaylistImage"
                android:layout_width="@dimen/playlist_dimen"
                android:layout_height="@dimen/playlist_dimen"
                android:contentDescription="@null"
                android:scaleType="centerCrop"
                android:src="@drawable/placeholder_icon"
                tools:background="@color/black" />

        </androidx.cardview.widget.CardView>

        <TextView
            android:id="@+id/tvPlaylistName"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:ellipsize="end"
            android:fontFamily="@font/ys_display_medium"
            android:maxLines="1"
            android:padding="2dp"
            android:textSize="12sp"
            tools:text="Название плейлиста аааааааааааааааааааа" />

        <TextView
            android:id="@+id/tvPlaylistCount"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:ellipsize="end"
            android:fontFamily="@font/ys_display_medium"
            android:maxLines="1"
            android:padding="2dp"
            android:textSize="12sp"
            tools:text="99 треков" />

    </LinearLayout>

</LinearLayout>